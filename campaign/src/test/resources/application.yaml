spring:
  jpa:
    database-platform: com.skplanet.shopadvisor.dialect.ShopAdvisorH2Dialect
    hibernate:
      ddl-auto: create
    open-in-view: false

logging:
  level:
    org:
      hibernate:
        type:
          descriptor:
            sql: trace